name: 🐛 バグ報告 (Bug Report)
description: スクリプトの動作に問題がある場合はこちら (Report a bug or issue with the script)
title: "[Bug]: "
labels: ["bug", "triage"]
assignees:
  - tay2501

body:
  - type: markdown
    attributes:
      value: |
        ## バグ報告にご協力ありがとうございます！
        以下の情報を提供いただくことで、問題の特定と修正が迅速に行えます。

  - type: checkboxes
    id: prerequisites
    attributes:
      label: 事前確認 (Prerequisites)
      description: バグを報告する前に、以下を確認してください。
      options:
        - label: README.md のトラブルシューティングセクションを確認した
          required: true
        - label: 既存の Issue で同じ問題が報告されていないか検索した
          required: true
        - label: 最新版のスクリプトを使用している
          required: true

  - type: textarea
    id: description
    attributes:
      label: 問題の説明 (Bug Description)
      description: 何が起きているか、簡潔に説明してください。
      placeholder: |
        例: スクリプトを実行すると "Failed to download filter list" エラーが出る
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: 期待される動作 (Expected Behavior)
      description: 本来どうあるべきか説明してください。
      placeholder: |
        例: 280blocker のフィルタリストがダウンロードされて、/var/opt/adguardhome/filters/280blocker_domain.txt に保存される
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 再現手順 (Steps to Reproduce)
      description: 問題を再現するための手順を記載してください。
      placeholder: |
        1. `sudo adguardhome-280blocker-filter-update -v` を実行
        2. エラーメッセージが表示される
        3. ...
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: 環境情報 (Environment)
      description: |
        以下のコマンドを実行して、結果を貼り付けてください：
        ```bash
        echo "OS: $(uname -a)"
        echo "Bash: $BASH_VERSION"
        echo "curl: $(curl --version | head -n 1)"
        echo "Script: $(which adguardhome-280blocker-filter-update || echo 'Not installed')"
        ```
      placeholder: |
        OS: Linux raspberrypi 6.1.0-rpi4-rpi-v8 #1 SMP PREEMPT Debian 1:6.1.54-1+rpt2 (2023-10-05) aarch64 GNU/Linux
        Bash: 5.1.16(1)-release
        curl: curl 7.88.1 (aarch64-unknown-linux-gnu)
        Script: /usr/local/bin/adguardhome-280blocker-filter-update
      render: shell
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: ログ/エラーメッセージ (Logs/Error Messages)
      description: |
        詳細モードで実行したログを貼り付けてください：
        ```bash
        sudo adguardhome-280blocker-filter-update -v
        ```
        または syslog のエラーログ：
        ```bash
        sudo grep adguardhome-280blocker-filter-update /var/log/syslog | tail -20
        ```
      placeholder: |
        [ERROR] Failed to download filter list. Neither 202601 nor 202512 were available.
        [ERROR] Please check network connectivity and 280blocker.net availability.
      render: shell
    validations:
      required: false

  - type: textarea
    id: additional
    attributes:
      label: 追加情報 (Additional Context)
      description: その他、問題の理解に役立つ情報があれば記載してください。
      placeholder: |
        - ネットワーク環境（プロキシ、ファイアウォールなど）
        - 過去に正常に動作していたか
        - 最近行ったシステム変更など
    validations:
      required: false
